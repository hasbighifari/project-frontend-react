const styles = theme => ({
    todo: {
        alignItems: 'center',
        backgroundColor: '#fff',
        borderRadius: '3px',
        boxShadow: '1px 1px 1px rgba(0, 0, 0, 0.15)',
        display: 'flex',
        fontSize: '18px',
        justifyContent: 'space-between',
        marginBottom: '10px',
        marginTop: '10px',
        padding: '20px 10px',
        fontWeight: 'bold'
    }
})
export default styles